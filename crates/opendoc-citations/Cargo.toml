[package]
name = "opendoc-citations"
version = "0.1.0"
edition = "2021"

[dependencies]
opendoc-core = { path = "../opendoc-core" }
hayagriva = { version = "0.10.1", default-features = false, features = ["biblatex", "archive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
