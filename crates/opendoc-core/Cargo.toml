[package]
name = "opendoc-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.11.0"

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3"
