[package]
name = "opendoc-merge"
version = "0.1.0"
edition = "2021"

[dependencies]
opendoc-citations = { path = "../opendoc-citations" }
opendoc-core = { path = "../opendoc-core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
opendoc-format = { path = "../opendoc-format" }
