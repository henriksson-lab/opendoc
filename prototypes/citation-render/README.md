# Citation Render Prototype

Run:

```sh
cargo run -p citation-render
```

This std-only prototype demonstrates structured citation nodes and bibliography rerendering. It is not a complete CSL implementation.
