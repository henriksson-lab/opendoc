# Rich Text CRDT Prototype

Run:

```sh
cargo run -p richtext-crdt
```

This is a std-only convergence sketch. It models concurrent text insertion, overlapping marks, and stable citation anchors. It is not a replacement for an Automerge-backed prototype.
