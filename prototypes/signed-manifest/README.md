# Signed Manifest Prototype

Run:

```sh
cargo run -p signed-manifest
```

This std-only prototype demonstrates the signed-manifest boundary and tamper failure. It uses a deterministic toy hash/MAC so it can run without dependencies. It is not cryptographic signing.
