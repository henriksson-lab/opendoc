# Spreadsheet Grid Prototype

Run:

```sh
cargo run -p spreadsheet-grid
```

This std-only prototype demonstrates stable row IDs, concurrent row insertion convergence, and formula references that can be projected back into visible row order.
